# About

All my talks, in all their glory, kindly made with [RevealJS](https://revealjs.com/).

# Structure

[`./common`](`./common`) contains all the "framework" code, that is then symlinked in each talk directory.

Talk contents is in Markdown format, in a `slides.md` file, following RevealJS expectations.

# Requirements

Node.js >= v22
